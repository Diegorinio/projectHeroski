using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class knightU:Unit
{
    public override void Awake()
    {
        base.Awake();
        unitName="Knight";
        unitBaseDamage=5;
        unitBaseHealth=20;
        gridDistance=3;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
